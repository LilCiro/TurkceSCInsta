# turkcescinsta projesini derleyip paketleyen iş akışı

name: turkcescinsta'yı Derle ve Paketle

on:
  workflow_dispatch:

jobs:
  build-rootless:
    runs-on: macos-13
    steps:
      - uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: latest-stable
          
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Checkout theos/theos
        uses: actions/checkout@v4
        with:
          repository: theos/theos
          ref: master
          submodules: recursive
          path: theos

      - name: Checkout SDKs
        uses: actions/checkout@v4
        with:
          repository: theos/sdks
          ref: master
          path: theos/sdks

      - name: Install dependencies
        run: brew install make xz ldid

      - name: Build package (rootless)
        run: |
          # Proje dosyaları ana dizinde olduğu için "cd main" komutu kaldırıldı.
          THEOS=theos gmake clean package FINALPACKAGE=1 THEOS_PACKAGE_SCHEME=rootless

      - name: Upload a Build Artifact (rootless)
        uses: actions/upload-artifact@v4
        with:
          name: turkcescinsta_packages_iphoneos-arm64 (rootless)
          path: packages/*_iphoneos-arm64.deb

  build-rootful:
    runs-on: macos-12
    steps:
      - uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: '14.2'
          
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Checkout theos/theos
        uses: actions/checkout@v4
        with:
          repository: theos/theos
          ref: master
          submodules: recursive
          path: theos

      - name: Checkout SDKs
        uses: actions/checkout@v4
        with:
          repository: theos/sdks
          ref: master
          path: theos/sdks

      - name: Install dependencies
        run: brew install make xz ldid

      - name: Build package (rootful)
        run: |
          # Proje dosyaları ana dizinde olduğu için "cd main" komutu kaldırıldı.
          THEOS=theos gmake clean package FINALPACKAGE=1

      - name: Upload a Build Artifact (rootful)
        uses: actions/upload-artifact@v4
        with:
          name: turkcescinsta_packages_iphoneos-arm (rootful)
          path: packages/*_iphoneos-arm.deb
